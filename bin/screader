#!/usr/bin/env node

const { watch } = require('../lib/screader')
const { program }= require('commander')

program
  .option('--reset', 'Reset all configuration')

program.parse()

watch(program.opts())
